# Data Domain

This directory is the future home for extension datasets and schema definitions.

## Subdirectories

- `source/`: hand-authored source data
- `generated/`: build-time generated data
- `schemas/`: validation schemas for the data contracts
