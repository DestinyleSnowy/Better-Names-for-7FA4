# Popup Domain

This domain replaces the nested `submitter/submitter/` popup package.

Planned responsibilities:

- popup bootstrap
- local popup state
- submitter API client
- popup UI components and styles
