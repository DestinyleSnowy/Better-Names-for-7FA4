# Background Domain

This domain replaces the monolithic legacy `background.js`.

Planned slices:

- lifecycle bootstrap
- message routing
- storage-backed preference services
- dynamic network rules
- submitter popup state
