# Panel Domain

This domain replaces the current injected panel assets under `content/panel/`.

Planned responsibilities:

- bootstrap and host integration
- state container
- UI components
- HTML templates
- CSS modules or layered stylesheets
